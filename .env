# 1. ลบไฟล์
rm .env

# 2. บอก Git ให้ไม่ track มันอีก
echo ".env" >> .gitignore

# 3. ลบออกจาก history Git (เพื่อไม่ให้คนย้อนดูได้)
git rm --cached .env
git commit -m "Remove .env file from repository"
git push

